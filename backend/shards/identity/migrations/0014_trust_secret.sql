-- Доверенное устройство (2FA) больше не опознаётся по голому device_id:
-- каталог отдаёт device_id любому авторизованному (identity.prekeys.fetch),
-- и второй фактор обходился при известном пароле. Теперь доверие — секрет,
-- выданный один раз после подтверждения в Telegram; здесь его SHA-256.
-- Старые строки без хэша доверенными НЕ считаются (одно повторное
-- подтверждение после обновления).
ALTER TABLE trusted_devices ADD COLUMN secret_hash TEXT NOT NULL DEFAULT '';
