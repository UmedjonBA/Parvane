{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. App-defined commands (login, send_text, sync_messages, nats_status) are always allowed; this grants the core defaults.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
