# Rust build artifacts (после переноса workspace в backend/; /target — старые клоны)
/backend/target
/target

# SQLite базы (создаются шардами при запуске)
*.db
*.db-shm
*.db-wal

# Конфиги окружения
.env

# Local workspace metadata
.agents/
.codex/
.claude/
# Локальные тестовые и демонстрационные профили (tdata, логи, SQLite)
/local-workdirs/
AGENTS.md
CLAUDE.md

# Плановые/стратегические доки — локальные, не для публичного репо
# (уже закоммичены локально; в исключениях, чтобы не уходили в GitHub)
/ARCHITECTURE.md
/ROADMAP.md
/specs/

# Node-зависимости корневых e2e-скриптов
node_modules/

# Байткод Python (телеграм-бот)
__pycache__/
*.pyc

# Android (Parvane): тяжёлые артефакты и клоны для изучения — не коммитить
/android/.build/
/android/prebuilt/
/android/_study/
