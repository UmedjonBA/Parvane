# Пароли NATS-юзеров для server.prod.conf. Реальный .env — НЕ в git.
# Экспортировать в окружение перед запуском nats-server -c server.prod.conf.
PARVANE_IDENTITY_PASS=change-me-identity
PARVANE_MESSENGER_PASS=change-me-messenger
PARVANE_CLOUD_PASS=change-me-cloud
PARVANE_CALL_PASS=change-me-call
PARVANE_GATEWAY_PASS=change-me-gateway
